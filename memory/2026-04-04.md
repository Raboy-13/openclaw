# Daily Notes — 2026-04-04

## CoCBot Development Session

### CoCBot Project
- **Location:** `C:\Users\daver\Documents\GitHub\coc-bot`
- **Language:** Python 3
- **Runtime:** ADB-based (Android emulator)
- **Architecture:** Modular MVC — `src/core`, `src/logic`, `src/ui`

### MyBot PHMOD v10 Comparison
- **Location:** `C:\Users\daver\Downloads\MyBot PHMOD v10\MyBot`
- **Language:** AutoIt (compiled .exe)
- **Size difference:** MyBot is much smaller because it uses PNG template matching (bitmap sprites) — no ML libraries needed. CoCBot is larger due to numpy, opencv-python, PaddleOCR etc.
- **OCR approach:** MyBot uses bitmap font strips — PNG templates of each digit matched via pixel comparison. CoCBot uses ML-based OCR which is heavier but more flexible.
- **CoCBot's weak point:** Digit OCR for wall counts — confuses 2↔8, wrong font/templates for builder menu UI.

### Wall Count OCR Fix (IMPLEMENTED)
- **Templates:** `assets/templates/digits/wall_count/0.png` through `9.png` — captured by Raboy from in-game builder menu, transparent backgrounds, 13x17 native size
- **Problem:** `trim_digit_lane=True` + complex HSV thresholding was merging the "1" digit with adjacent "Walls" text into one blob, causing "1" to fail
- **Fix:** New OCR context `"wall_count"` → uses `_threshold_wall_count_digits()` (simple grayscale Otsu) + `trim_digit_lane=False`
- **Files changed:**
  - `src/core/ocr/preprocessing.py` — added `_threshold_wall_count_digits()`
  - `src/core/ocr/resources.py` — threaded `ocr_context` param through: `read_home_numeric_value` → `_read_home_numeric_value` → `run_home_ocr_attempt`
  - `src/logic/wall/upgrader/selection.py` — wall count OCR uses `ocr_context="wall_count"`

### Wall Upgrader Known Issue (pending fix)
- **Post-upgrade resource refresh is expensive:** After each upgrade, `perform_upgrade()` calls `_load_home_resource_amounts()` which does a full screen OCR read. Should instead deduct the known upgrade price from cached values and only re-scan the wall price.
- **Max-level wall blocks confirmation:** When a wall is at TH-max level (e.g. TH11 = level 11 max), tapping confirm triggers a "Note!" popup. Bot doesn't detect this and gets stuck on confirmation screen.
- **Fix plan (NOT YET IMPLEMENTED):**
  1. Before tapping confirm — scan for "Note!" text (template: `wall_note_note.png`)
  2. If "Note!" found → tap close button → restart wall loop to find different wall
  3. If "Note!" not found → proceed to tap confirm
  4. For multi-wall path: also scan `multi_wall_upgrade_confirm.png` if in multi-wall flow

### Calibration Overlay Tool (IN PROGRESS)
- **Claude Code session:** `wild-falcon` (running)
- **Purpose:** Live calibration tool for wall upgrade Note/confirmation scan areas
- **Templates to calibrate:**
  - `assets/templates/wall/wall_upgrade_close.png` — close button
  - `assets/templates/wall/wall_note_note.png` — "Note!" text
  - `assets/templates/wall/confirm_upgrade.png` — confirm button (tap area if Note not found)
  - `assets/templates/wall/multi_wall_upgrade_confirm.png` — multi-wall confirm
- **Features:** Pan/zoom, transparent overlays locked to template pixel sizes, real-time confidence display
- **Output file:** `C:\Users\daver\Documents\GitHub\coc-bot\wall_note_calibration.py`

### Key Code Locations
- Wall upgrader main: `src/logic/wall/upgrader/`
- OCR: `src/core/ocr/resources.py`, `src/core/ocr/preprocessing.py`
- Template matching: `src/core/ocr/templates.py`
- Bot controller: `src/logic/bot/controller.py`
- Config: `config/` (YAML)
- Debug output: `debug/ocr/`

## Session Notes
- Raboy is @that_raboy on Telegram, GMT+8 timezone
- Bot built slow (one-packaged-exe workflow) — fixes need to be right first time
- Code Explorer → Omaygot workflow for complex fixes
